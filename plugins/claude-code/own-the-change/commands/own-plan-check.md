---
description: Preview the likely scope, risks, and tests for a requested change.
---

Run `python3 "${CLAUDE_PLUGIN_ROOT}/scripts/read_protocol.py" --mode plan` and follow its output. If unavailable, read `${CLAUDE_PLUGIN_ROOT}/docs/protocol/understanding-protocol.md`.

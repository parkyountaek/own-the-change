---
description: Try a synthetic debrief without sharing project code or cloning the source.
---

Run `python3 "${CLAUDE_PLUGIN_ROOT}/scripts/read_protocol.py" --mode demo` and follow its output. If unavailable, read `${CLAUDE_PLUGIN_ROOT}/docs/protocol/understanding-protocol.md`.

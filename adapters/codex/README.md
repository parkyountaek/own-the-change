# Codex 어댑터

Codex는 저장소 루트의 `.agents/skills/own-the-change`을 직접 읽을 수 있다. 이 심볼릭 링크는 공통 skill인 `../../skills/own-the-change/`를 가리킨다. 배포용 plugin metadata는 `../../.codex-plugin/plugin.json`에 둔다.

- 프로젝트 범위 발견: `../../.agents/skills/own-the-change`
- 사용자 범위 설치: `../../scripts/install-local.sh codex-user`
- 공통 skill: `../../skills/own-the-change/SKILL.md`
- 학습 규칙 정본: `../../docs/protocol/understanding-protocol.md`

공통 학습 규칙을 이 파일에 복사하지 않는다.

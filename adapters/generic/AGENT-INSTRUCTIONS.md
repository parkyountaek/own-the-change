# Generic agent instructions

Resolve this file's real path, following symbolic links. Its layout is `<source-root>/adapters/generic/AGENT-INSTRUCTIONS.md`. Read `<source-root>/docs/protocol/understanding-protocol.md`, including **Runtime language**, and follow the requested checkpoint section; it is the only source of learning rules.

Run `python3 <source-root>/scripts/resolve_context.py --target <current-working-directory>` for the target Git root and source resource paths. This adapter requires the full local source checkout; do not resolve its template or validator relative to the target repository.

#!/usr/bin/env sh
# This hook only suggests an optional next step. It never writes a record.
printf '%s\n' 'Own The Change: if this coding task changed files, you may run /own-change-debrief and /own-understanding-check.'
exit 0

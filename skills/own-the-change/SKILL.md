---
name: own-the-change
description: Explain and check AI code changes with optional local records. Use for requested Own The Change checkpoints, its synthetic demo, or plugin environment diagnosis.
---

From this file's real directory (follow links), run `python3 ../../scripts/read_protocol.py --mode MODE` (default debrief, or plan/understanding/demo/doctor). Follow it; on failure read [the protocol](../../docs/protocol/understanding-protocol.md).

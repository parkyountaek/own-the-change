---
name: own-demo
description: Try Own The Change on a bundled synthetic change in a fresh temporary Git repository, without using project code.
---

From this file's real directory (follow links), run `python3 ../../scripts/read_protocol.py --mode demo`. Follow its output; on failure read [the protocol](../../docs/protocol/understanding-protocol.md).
